A containerised ETA Predictor which comprises of a trained machine learning model that predicts package delivery times for a logistics company. 
It has been wrapped in a professional REST API using FastAPI and packaged inside a Docker container so it runs identically on any machine in the world. 
Every training experiment has been tracked in MLflow.